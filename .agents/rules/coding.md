# Coding Standards

*   **Java/Spring Boot:** Use standard Spring REST and WebSocket conventions.
*   **Security:** Ensure passwords are not double-hashed on entity updates to prevent authentication failures. Validate password logic in user update services.
*   **React:** Use functional components and hooks. Maintain clean WebSocket lifecycle management (connect on mount, disconnect on unmount).
